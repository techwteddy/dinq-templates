// Minimal stub to satisfy TS in backend build.
declare module "mapbox__point-geometry" {
  // We don't use this in the backend; keep it loose.
  const PointGeometry: any;
  export default PointGeometry;
}

