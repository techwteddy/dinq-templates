// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

/* header - array of first or second
interface MobileMenuCollapsibleProps {
  collapsible: true;
  title: string;
  links: Link[];
  content: {
    header: string;
    genericLink?: Link;
    description: string;
  };
}
interface MenuNormItemProps {
  collapsible: false;
  links: Link[];
}
 */

model HeaderMenuItem {
  id String @id @default(uuid())

  collapsible Boolean
  order Int @default(0)
  title String? // name of menu item
  content Json? // left part description (header, description, genericLink?)
  imagesHeader String?
  images HeaderImage[]

  // Relation: One Menu Item -> Many Links
  links HeaderLink[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model HeaderLink {
  id String @id @default(uuid())
  href String
  title String

  itemId String
  item HeaderMenuItem @relation(fields: [itemId], references: [id], onDelete: Cascade)
}

model HeaderImage {
  id String @id @default(uuid())
  title String
  href String
  imgUrl String
  imgAlt String

  itemId String
  item HeaderMenuItem @relation(fields: [itemId], references: [id], onDelete: Cascade)
}


/* 
image carousel - array of:
type CarouselCardsData = {
  about?: string;
  header: string;
  description: string;
};
 */

model CarouselCardData {
  id String @id @default(uuid())

  about String?
  header String
  description String
  imgUrl String
  imgAlt String
  order Int @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

/* 
experience cards - array of:
type ExperienceCardsData = {
  title: string;
  header: string;
  description: string;
};
 */
model ExperienceCardData {
  id String @id @default(uuid())

  title String
  header String
  description String
  order Int @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

/* 
footer
  type FooterList = {
  header: string;
  items: {
    title: string;
    href: string;
  }[];
};
 */
model FooterColumn {
  id String @id @default(uuid())
  header String
  order Int @default(0)

  items FooterLink[]
}

model FooterLink {
  id String @id @default(uuid())
  href String
  title String

  columnId String
  column FooterColumn @relation(fields: [columnId], references: [id], onDelete: Cascade)
}

/*
admin
  name
  username
  password
*/
model Admin {
  id String @id @default(uuid())

  name String
  username String @unique
  password String
  tokenVersion Int @default(0)

  createdAt DateTime @default(now())
}

/*
  form messages
*/

model Messages {
  id String @id @default(uuid())

  firstName String
  lastName String
  email String
  phone String?
  country String?
  about String
  message String
  replied Boolean @default(false)
  replies Replies[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Replies {
  id String @id @default(uuid())

  message String
  createdAt DateTime @default(now())

  messageId String
  userMessage Messages @relation(fields: [messageId], references: [id], onDelete: Cascade)
}
