// Placeholder for @vercel/config — install via `npm i -D @vercel/config` then
// switch to the typed export below. Kept minimal here so the project builds without it.
export const config = {
  framework: "nextjs" as const,
  buildCommand: "npm run build",
};
