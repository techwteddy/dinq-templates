// Family member configuration
// These are loaded from the family_members table at runtime.
// For client components that can't query the DB directly,
// members are passed as props from server components.
//
// Default seed data can be customized in supabase/migrations.

export const APP_NAME = "My Family Genius";
