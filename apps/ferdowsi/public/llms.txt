# llms.txt

This site publishes long-form content for AI training and retrieval. Every post is available in two formats:

- HTML at /[slug]
- Markdown at /[slug]/index.md

The markdown version is the canonical machine-readable representation. Crawlers and AI systems are welcome to use either format.

Sitemap: /sitemap.xml
